# 文档生成视频自动化工作流 — 技术方案书

> **版本:** v0.1(待评审)
> **日期:** 2026-08-27
> **状态:** 草稿 → 提交 gpt-5.6-sol 技术审查
> **目标读者:** 实现者(零上下文可开发)

**目标:** 构建一条"文档 → 视频"的全自动流水线:输入一份文档(PDF/Word/Markdown/TXT),无人值守输出一条带口播、配图、字幕的成片(MP4)。

**架构:** 8 阶段流水线(P0–P7),每阶段输入/输出结构化 JSON 文件(数据契约驱动),支持断点续跑、素材缓存、供应商可插拔(本地/云混合)。

**技术栈:** Python 3.11 + uv · PyMuPDF/python-docx(解析)· Ollama Qwen3 + 云 API(LLM)· edge-tts(语音)· FAL FLUX 2(图像)· ffmpeg(合成)· faster-whisper(字幕对齐)· asyncio 编排。

---

## 目录

1. [项目概述](#1-项目概述)
2. [需求分析](#2-需求分析)
3. [总体架构](#3-总体架构)
4. [模块详细设计](#4-模块详细设计)
5. [技术选型](#5-技术选型)
6. [数据契约与中间产物](#6-数据契约与中间产物)
7. [编排与自动化](#7-编排与自动化)
8. [开发计划](#8-开发计划)
9. [质量保障与验收](#9-质量保障与验收)
10. [风险与对策](#10-风险与对策)
11. [附录](#11-附录)

---

## 1. 项目概述

### 1.1 背景与目标

将文档内容自动转化为讲解视频,用于知识科普、报告解读、培训资料、公众号文章视频化等场景。核心价值:**全流程无人值守**,一条命令从文档到成片;中间产物结构化留存,可审可改。

### 1.2 范围

**本期包含:**

- 文档解析(PDF/DOCX/MD/TXT,含结构识别)
- 内容理解与讲稿生成(LLM)
- 分镜规划与图像提示词生成
- 语音合成(TTS)、图像素材生成与缓存
- 视频合成(Ken Burns 动效 + 旁白 + 背景音乐 + 字幕)
- 字幕生成与时间轴对齐
- 质检与报告、CLI、批量/监听模式、断点续跑

**本期不包含(后续可选增强):**

- 数字人播报(Avatar)
- 复杂动画(Manim/Remotion 动效模板)
- 视频片段级 AI 生成(FLUX3 text-to-video,可作为 P5 图像素材的可选替换)
- Web 管理界面(二期)
- 多机分布式执行

### 1.3 关键假设

| # | 假设 | 说明 |
|---|------|------|
| A1 | 视频形态为**图文解说视频**:口播 + 静态配图(带缓慢动效)+ 字幕 | 非数字人/非动画 |
| A2 | 内容语言以**中文**为主 | 多语言走配置项,不写死 |
| A3 | 运行环境:Windows 11 主机,RTX 5070 12GB,Ollama 已装,FAL 订阅可用 | 见 5.3 环境要求 |
| A4 | 单条视频默认时长 **1–10 分钟**,按文档长度自适应 | 超长文档自动分集(可配) |
| A5 | 图像素材优先走云端 FAL;本地生成(ComfyUI)为可选降级路径 | 成本/速度权衡 |

### 1.4 术语表

| 术语 | 含义 |
|------|------|
| Job | 一次"文档 → 视频"作业,对应 workspace 下一个目录 |
| Scene(分镜) | 一段旁白 + 一张画面 + 一段字幕的最小合成单元 |
| 中间产物 | parsed.json / summary.json / script.json / scenes.json 等阶段输出 |
| Provider | LLM/TTS/Image/Composer 的可插拔供应商适配器 |
| Ken Burns | 静态图缓慢缩放/平移的镜头动效(ffmpeg zoompan 实现) |

---

## 2. 需求分析

### 2.1 功能需求

| 编号 | 需求 | 验收要点 |
|------|------|----------|
| FR-01 | 接收文档(PDF/DOCX/MD/TXT),提取文本与结构(标题层级/段落/列表/表格/图片占位) | 三种格式样例解析通过,结构树正确 |
| FR-02 | 长文档自动分章节,生成全文摘要与要点 | 摘要覆盖章节,要点 ≤ 10 条 |
| FR-03 | 生成分镜讲稿:每段口播 15–40 秒,总时长受控 | 每场景 60–180 字;总时长偏差 < 20% |
| FR-04 | 每分镜生成画面描述与图像提示词,风格全局统一 | 提示词含风格前缀,不含画面文字 |
| FR-05 | 语音合成:中文口播,音色/语速可配 | 音频无静音超段,格式 mp3/m4a |
| FR-06 | 图像素材生成 + 缓存(同提示词去重) | 缓存命中时不重复调用生成 API |
| FR-07 | 视频合成:画面动效 + 旁白 + 背景音乐(可选)+ 字幕 | 输出 H.264/AAC MP4,画面-音频-字幕三同步 |
| FR-08 | 字幕生成与时间轴对齐(ASR 回读对齐) | 字幕与语音偏差 < 300ms |
| FR-09 | 质检:可播放性/时长/静音/黑帧/字幕覆盖检查,产出报告 | qc_report.json + 人类可读摘要 |
| FR-10 | CLI:单文件、批量、watch 监听、断点续跑、失败重试 | 中断后 `resume` 不重复已完成的阶段 |

### 2.2 非功能需求

| 编号 | 需求 |
|------|------|
| NFR-01 | 全自动无人值守,失败可重跑,不丢已产出的素材 |
| NFR-02 | 断点续跑:任意阶段失败可从中断处继续 |
| NFR-03 | 可插拔:LLM/TTS/图像/合成各供应商可配置切换 |
| NFR-04 | 可观测:结构化日志 + 每 Job 的 state.json 进度 |
| NFR-05 | 成本可控:素材缓存、本地 LLM/whisper 优先、云端仅做必要调用 |
| NFR-06 | 性能:5000 字文档端到端 ≤ 10 分钟(不含云端图像排队) |
| NFR-07 | Windows 优先、代码跨平台;单机运行,无外部服务依赖(除供应商 API) |

### 2.3 输入/输出规格

**输入:** `*.pdf / *.docx / *.md / *.txt`(pptx 一期不支持,可先转 PDF)。单文件 ≤ 200MB。扫描件 PDF 走 OCR 降级路径(可选)。

**输出(每个 Job):**

| 产物 | 规格 |
|------|------|
| 成片 | MP4,H.264 High, AAC 128k,25fps,1920×1080(横屏,默认)或 1080×1920(竖屏,可选) |
| 字幕 | SRT + ASS 双份;默认烧录硬字幕进成片(可配置关闭) |
| 质检报告 | qc_report.json + markdown 摘要 |
| 中间产物 | 全部 JSON + 素材文件(见第 6 章) |

### 2.4 典型场景

- **S1 报告解读:** 一份 30 页技术报告 PDF → 5 分钟横屏解说视频
- **S2 文章批量视频化:** 一个文件夹 20 篇公众号文章(MD)→ 批量竖屏短视频,watch 模式增量处理
- **S3 培训资料:** PPT 导出 PDF → 分章节讲解视频(表格/图片转画面描述)

---

## 3. 总体架构

### 3.1 架构总览

```mermaid
flowchart LR
    A[输入文档] --> B["P0 接收与规范化<br/>ingest"]
    B --> C["P1 文档解析<br/>parsed.json"]
    C --> D["P2 内容理解<br/>summary.json"]
    D --> E["P3 讲稿生成<br/>script.json"]
    E --> F["P4 分镜规划<br/>scenes.json"]
    F --> G["P5 素材生产<br/>音频+图像(并行)"]
    G --> H["P6 合成与字幕<br/>ffmpeg + whisper"]
    H --> I["P7 质检与交付<br/>output.mp4 + 报告"]

    style G fill:#f9f,stroke:#333
    style H fill:#f9f,stroke:#333
```

### 3.2 流水线阶段

| 阶段 | 名称 | 输入 | 输出 | 关键工具 | 并行性 |
|------|------|------|------|----------|--------|
| P0 | 接收与规范化 | 原始文档路径 | `input/` 文档副本 + manifest.json | shutil, pandoc(可选) | — |
| P1 | 文档解析 | 文档文件 | `parsed.json` | PyMuPDF / python-docx / rapidocr | — |
| P2 | 内容理解 | parsed.json | `summary.json` | LLM(分块 map-reduce) | 按章节并行 |
| P3 | 讲稿生成 | summary.json | `script.json` | LLM | 按章节并行 |
| P4 | 分镜规划 | script.json | `scenes.json` | LLM + 规则 | 按场景并行 |
| P5 | 素材生产 | scenes.json | `assets/audio/*.mp3`, `assets/images/*.png` | edge-tts / FAL / 本地 | **高并行**(信号量限流) |
| P6 | 合成与字幕 | scenes.json + 素材 | `final/output.mp4`, `subtitles.*` | ffmpeg / faster-whisper | — |
| P7 | 质检与交付 | final/ | `qc_report.json` + 摘要 | ffprobe / volumedetect | — |

### 3.3 设计原则

1. **数据契约驱动:** 阶段间只通过 JSON 文件通信,不传内存对象 → 任何阶段可独立重跑、独立测试、独立替换。
2. **幂等 + 断点续跑:** 每阶段产物落盘并记录输入哈希;重跑时输入哈希未变则跳过(见 6.3)。
3. **可插拔 Provider:** 所有外部能力(LLM/TTS/图像/合成)走统一接口,`config/default.yaml` 切换供应商。
4. **本地云混合(成本优先):** LLM 默认本地 Qwen3(Ollama),失败/质量不足时可切云端;whisper 本地;TTS 走 edge-tts(免费);图像走 FAL(订阅已付费)。
5. **失败降级:** 单场景图像失败 → 占位图 + 报告标注,不阻断整条流水线;LLM 输出 JSON 损坏 → 自动修复 + 重试。
6. **可观测:** 每阶段写 state.json 进度 + 结构化日志(时间/阶段/耗时/重试次数)。

### 3.4 数据流(端到端)

```
report.pdf
  → P1 parsed.json {title, sections[].blocks[]}
  → P2 summary.json {doc_summary, key_points[], chapter_plan[]}
  → P3 script.json {scenes[{id, narration, est_duration_s}]}
  → P4 scenes.json {scenes[{id, narration, visual_desc, image_prompt, style, layout}]}
  → P5 assets/audio/{id}.mp3 + assets/images/{id}.png (实测音频时长回写 scenes.json)
  → P6 final/output.mp4 + subtitles.srt/.ass
  → P7 qc_report.json
```

---

## 4. 模块详细设计

### 4.1 P1 文档解析器 `parser`

**职责:** 把异构文档转成统一的 `parsed.json` 结构树。

**输入:** 文档文件路径 + 类型探测(按扩展名 + magic 判定)。

**输出 `parsed.json` 结构:**

```json
{
  "meta": { "source": "report.pdf", "type": "pdf", "pages": 30, "chars": 12500 },
  "title": "2026 年 XX 技术报告",
  "sections": [
    {
      "id": "s1", "level": 1, "heading": "第一章 概述",
      "blocks": [
        { "type": "paragraph", "text": "..." },
        { "type": "list", "items": ["a", "b"] },
        { "type": "table", "caption": "表1", "rows": [["列1","列2"],["1","2"]] },
        { "type": "image", "alt": "图1", "path": "assets/extracted/img_001.png" }
      ]
    }
  ]
}
```

**实现要点:**

| 格式 | 方案 | 库 |
|------|------|----|
| PDF(文本型) | 逐页提取文本,按字号/加粗启发式识别标题层级;无层级时按"段落空行 + 首句关键词"分节 | PyMuPDF |
| PDF(扫描型) | 检测无文本层 → OCR 降级路径(rapidocr-onnxruntime 本地,或 marker 高质量可选) | rapidocr / marker-pdf(可选) |
| DOCX | 样式名映射(Heading 1-6 → level),表格/图片提取 | python-docx |
| MD/TXT | 原生结构 | markdown-it-py / 内置 |

**接口签名:**

```python
# src/doc2video/pipeline/p1_parser.py
def parse(path: Path, cfg: Config) -> ParsedDocument        # 纯函数,返回 dataclass
def detect_type(path: Path) -> str                          # pdf|docx|md|txt|scanned_pdf
def save_parsed(doc: ParsedDocument, out: Path) -> None     # 写 parsed.json
```

**错误处理:** 加密 PDF → 报错进报告;单页解析失败 → 跳过该页并记录页码;OCR 失败 → 该 PDF 标记 `scanned_unsupported` 且 Job 失败。

### 4.2 P2 内容理解 `understander`

**职责:** 生成全文摘要、要点、分章计划,供讲稿生成使用。

**输出 `summary.json`:**

```json
{
  "doc_summary": "本文介绍……(150–300 字)",
  "key_points": ["要点1", "要点2"],
  "chapter_plan": [
    { "chapter": "概述", "section_ids": ["s1", "s2"], "chars": 2100, "planned_scenes": 4 }
  ]
}
```

**长文档策略(核心难点):**
1. 按 section 聚合,每块 ≤ 12,000 字符(≈ 6k token);
2. 逐块摘要(map)→ 归并总结(reduce);
3. `planned_scenes = clamp(round(chars / 500), 2, 20)` — 按 500 字/场景粗估,上限 20 场景(约 8 分钟)。

**接口:** `understand(parsed: ParsedDocument, llm: LLMProvider) -> Summary`

### 4.3 P3 讲稿生成 `script_writer`

**职责:** 把章节计划变成逐场景口播稿。**本项目的质量核心。**

**输出 `script.json`:**

```json
{
  "scenes": [
    { "id": "sc01", "chapter": "概述", "narration": "大家好,本期我们解读……", "est_duration_s": 28.4 }
  ]
}
```

**生成规则(写入 Prompt,见附录 B):**

- 口语化中文,避免书面长句;首场景固定为开场白(自我介绍 + 文档标题 + 看点预告),末场景为总结 + 结尾语;
- 每场景 60–180 字(≈ 15–40 秒),按语速 4.5 字/秒估算 `est_duration_s`;
- 场景间有逻辑递进,不重复信息;
- 输出严格 JSON(见附录 B 的 Prompt 模板),schema 校验失败 → 自动修复(截取/补全)→ 重试 ≤ 3 次。

**时长约束:** 全片 ≤ `config.max_duration_s`(默认 600s)时按计划执行;超限 → 让 LLM 压缩到目标场景数内。

**接口:** `write_script(summary: Summary, llm: LLMProvider, cfg: Config) -> Script`

### 4.4 P4 分镜规划 `scene_planner`

**职责:** 为每场景生成画面描述与图像提示词。

**输出 `scenes.json`(合并 P3,增加视觉字段):**

```json
{
  "style": { "name": "flat-illustration", "prefix": "扁平插画风格, 配色以深蓝+橙色为主, 简洁构图, 无文字", "negative": "text, watermark, words, letters" },
  "scenes": [
    {
      "id": "sc01", "narration": "…", "est_duration_s": 28.4,
      "visual_desc": "一位讲解员站在大屏幕前,屏幕上展示文档封面",
      "image_prompt": "扁平插画风格,……,一位讲解员站在大屏幕前,……,无文字",
      "aspect": "16:9"
    }
  ]
}
```

**风格模板(`config/styles.yaml`):** `flat-illustration`(扁平插画)/ `business-minimal`(商务极简)/ `tech-dark`(科技深色)/ `realistic-photo`(写实)。模板提供 prefix/negative/默认色调。

**要点:** 图像提示词 = 风格前缀 + 场景 visual_desc + "无文字、无水印" 后缀;LLM 生成 visual_desc 时**禁止描述画面中的文字**。

**接口:** `plan_scenes(script: Script, llm: LLMProvider, style: StyleTemplate) -> Scenes`

### 4.5 P5 素材生产 `asset_producers`

**职责:** 并行生产每场景的音频与图像,并做缓存与失败降级。

**5.5.1 音频(TTS):**

| Provider | 实现 | 特点 |
|----------|------|------|
| edge-tts(默认) | `edge_tts.Communicate(text, voice).save()` | 免费、中文音色多(如 zh-CN-XiaoxiaoNeural/YunxiNeural) |
| OpenAI TTS(可选) | openai.audio.speech | 质量高、付费 |
| CosyVoice2(可选) | 本地推理(RTX 5070) | 免费离线、部署成本高 |

- 每场景合成独立 mp3;并发 4(信号量);
- **实测时长回写** scenes.json(合成以实测为准,不依赖估算);
- 失败重试 2 次 → 标为 `audio_failed`,P6 用静音占位 + 报告标注。

**5.5.2 图像:**

| Provider | 实现 | 特点 |
|----------|------|------|
| FAL FLUX 2 Klein(默认) | FAL API(`fal-ai/flux`) | 订阅已付费、质量高、快 |
| ComfyUI 本地(可选) | HTTP API 驱动本地 SDXL/FLUX | 免费、慢、占显存 |

- **缓存键:** `sha1(image_prompt + style + aspect + seed)`;命中直接复用(`assets/images/<key>.png` 已存在);
- 并发 2–4(API 限流保护);失败重试 2 次 → 降级为风格化占位图(纯色 + 场景标题文字,由 PIL 本地生成);
- 图像文件名与场景 id 的映射写入 scenes.json。

**接口:** `produce_assets(scenes: Scenes, providers, cfg) -> Scenes(回写实测时长/素材路径)`

### 4.6 P6 合成器 `compositor`

**职责:** 场景素材 → 成片。**基于 ffmpeg,不用 moviepy**(可重复性、性能、可控性都更好;moviepy 仅用于原型验证)。

**合成流程:**

1. **每场景片段:** 图像 + Ken Burns(zoompan 慢速放大/平移,方向按场景 id 哈希轮换)→ 片段视频(帧率 25,时长 = 实测音频时长 + 0.5s 前后留白)→ 与旁白音频合成 → 首尾各 300ms 淡入淡出;
2. **拼接:** concat demuxer 按场景顺序拼接;
3. **背景音乐(可选):** 循环混音,`volume=0.12`,结尾 fade-out;
4. **响度归一:** `loudnorm=I=-16:TP=-1.5`;
5. **字幕烧录:** ASS + `subtitles=...`(libass),样式按 style 模板配色;
6. **编码:** `-c:v libx264 -preset medium -crf 20 -c:a aac -b:a 128k -movflags +faststart`。

**核心 ffmpeg 命令示例(单场景 Ken Burns):**

```bash
ffmpeg -y -loop 1 -i scene01.png -i scene01.mp3 \
  -filter_complex "\
[0:v]scale=3840:2160,zoompan=z='min(zoom+0.0008,1.15)':x='iw/2-(iw/zoom/2)':y='ih/2-(ih/zoom/2)':d=25*DUR:s=1920x1080:fps=25,\
fade=t=in:st=0:d=0.3,fade=t=out:st=DUR-0.3:d=0.3[v];\
[1:a]apad[a]" \
  -map "[v]" -map "[a]" -t DUR \
  -c:v libx264 -preset medium -crf 20 -c:a aac -b:a 128k scene01.mp4
```

> 完整命令集见附录 C。`DUR` 由 P5 实测音频时长代入。

**接口:** `compose(scenes: Scenes, cfg: Config) -> Path`(返回 output.mp4 路径)

### 4.7 P7 字幕 `subtitler`

**职责:** 生成与语音精确对齐的字幕。

**两级策略:**

1. **初版(线性插值):** 旁白文本按字符比例映射到实测音频时间轴 → 生成 srt/ass;
2. **精对齐(默认开启):** faster-whisper(本地,`small` 模型,RTX 5070 上 ≈ 实时 5–10x)回读旁白音频 → 用 ASR 时间戳强制对齐原文(原文↔ASR 按字符相似度对齐,解决 ASR 误字);
3. **一致性检查:** 字幕起止时间必须落在对应音频范围内,越界自动裁剪。

**接口:** `make_subtitles(scenes: Scenes, cfg: Config) -> Subtitles`(输出 subtitles.json + .srt + .ass)

### 4.8 P7 质检 `qc`

**职责:** 交付前自动检查,产出报告。

**检查项:**

| 检查 | 方法 | 阈值 |
|------|------|------|
| 可播放性 | ffprobe 解析 | 必须通过 |
| 时长偏差 | 成片时长 vs scenes 实测音频总和 | ±5% |
| 静音占比 | `silencedetect` / `volumedetect` | 静音 > 2s 段落数 ≤ 1 |
| 黑帧/空白 | 抽帧亮度分析(PIL) | 黑帧占比 < 1% |
| 字幕覆盖 | 字幕条数 vs 场景数 | 覆盖率 100% |
| 素材降级数 | scenes.json 中 placeholder 计数 | 报告标注即可 |

**接口:** `run_qc(final_dir: Path, scenes: Scenes) -> QCReport`

---

## 5. 技术选型

### 5.1 各环节选型对比

| 环节 | 候选 | 结论 | 理由 |
|------|------|------|------|
| 文档解析 | **PyMuPDF** / pdfplumber / PDFMiner | PyMuPDF | 快、文本+字体信息全(层级推断需要字号数据) |
| DOCX 解析 | **python-docx** / pandoc | python-docx | 原生样式映射 |
| 扫描件 OCR | rapidocr-onnxruntime(本地)/ marker-pdf | rapidocr 为主,marker 可选 | 本地免费;marker 质量高但依赖重 |
| LLM | **Ollama qwen3:14b(本地默认)** / DeepSeek / Gemini-3.5-flash / GPT | 统一 LLMProvider,默认本地 | 成本 0;云端按需切换;OpenAI 兼容接口统一接入 |
| TTS | **edge-tts** / OpenAI TTS / CosyVoice2 | edge-tts | 免费、中文音色好、无部署成本 |
| 图像 | **FAL FLUX 2 Klein(默认)** / ComfyUI 本地 | FAL | 已付费订阅、速度快;本地为可选降级 |
| 视频合成 | **ffmpeg** / moviepy / Remotion | ffmpeg | 性能与可控性最好;命令模板化后可测试 |
| 字幕对齐 | **faster-whisper small** / large-v3 | faster-whisper small | 本地免费,RTX 5070 实时性好,对齐精度足够 |
| 编排 | **asyncio + state.json** / n8n / Prefect | asyncio | 零额外依赖,断点续跑逻辑自控;n8n 二期可视化可选 |
| 包管理 | **uv** / pip | uv | 用户环境已装,速度快 |
| LLM 客户端 | openai SDK(指向 Ollama /v1)/ ollama SDK | openai SDK 兼容层 | 一套代码同时接本地与云端 |

> ⚠️ **已知坑(实测):** 本地 Qwen3 走 OpenAI 兼容 `/v1` 接口时,思考模式会把答案放进 `reasoning_content` 而 `content` 返回空字符串。接入时须:①调用 Ollama 原生 `/api/chat` 并传 `think:false`;或 ②在 provider 适配层对空 content 检测并重试/降级云端。此坑必须写进 LLMProvider 的单元测试。

### 5.2 推荐技术栈汇总

```
Python 3.11 + uv
├── 解析: PyMuPDF, python-docx, markdown-it-py, rapidocr-onnxruntime(可选)
├── LLM: openai SDK + httpx(本地 Ollama 原生 API 适配)
├── TTS: edge-tts
├── 图像: FAL REST API(requests/httpx)
├── 合成: ffmpeg 6.x(系统安装,命令行调用)
├── 字幕: faster-whisper(ctranslate2)
├── 校验: jsonschema, pydantic
├── 测试: pytest, pytest-asyncio
└── 配置: pyyaml, 日志: stdlib logging(结构化)
```

### 5.3 环境与硬件要求

| 项 | 要求 |
|----|------|
| OS | Windows 11(代码保持跨平台) |
| Python | 3.11(uv 管理;用户机另有 3.14,依赖兼容以 3.11 为准) |
| ffmpeg | 6.x 加入 PATH(winget install ffmpeg) |
| GPU | RTX 5070 12GB:whisper small + qwen3:14b(量化)可并存;图像生成默认云端,本地可选 |
| Ollama | 已装,`qwen3:14b` 已拉取;需开 `OLLAMA_NUM_PARALLEL≥2` |
| 网络 | Ollama(localhost:11434)、edge-tts(微软 CDN)、FAL、可选 DeepSeek/Gemini API |
| 密钥 | FAL_KEY(env);云端 LLM key 可选(GOOGLE_API_KEY 等已配于 Hermes,可复用 env) |

---

## 6. 数据契约与中间产物

### 6.1 工作区目录结构

```
doc2video/
├── src/doc2video/
│   ├── cli.py                  # 命令行入口
│   ├── pipeline/
│   │   ├── p0_ingest.py
│   │   ├── p1_parser.py
│   │   ├── p2_understand.py
│   │   ├── p3_script.py
│   │   ├── p4_scenes.py
│   │   ├── p5_assets.py
│   │   ├── p6_compose.py
│   │   └── p7_qc.py
│   ├── providers/
│   │   ├── base.py             # LLMProvider/TTSProvider/ImageProvider 抽象
│   │   ├── llm_ollama.py / llm_openai_compat.py
│   │   ├── tts_edge.py / tts_openai.py
│   │   ├── img_fal.py / img_comfyui.py / img_placeholder.py
│   │   └── composer_ffmpeg.py
│   ├── schemas/                # jsonschema 定义(parsed/summary/script/scenes/subtitles)
│   ├── state.py                # 状态机 + 哈希断点
│   ├── cache.py                # 素材缓存(键=sha1)
│   ├── prompts/                # 附录 B 的 Prompt 模板(独立文件,可调)
│   └── qc/
├── tests/                      # pytest;fixtures 含 3 类样例文档;golden 端到端
├── examples/                   # 样例文档(pdf/docx/md)
├── config/
│   ├── default.yaml            # 供应商/并发/风格/时长等
│   └── styles.yaml             # 视觉风格模板
├── workspace/                  # 运行时生成(勿提交)
│   └── <job_id>/
│       ├── manifest.json       # 作业元信息(来源、参数、时间)
│       ├── state.json          # 阶段状态机(见 6.3)
│       ├── input/              # 原始文档副本
│       ├── parsed.json
│       ├── summary.json
│       ├── script.json
│       ├── scenes.json         # P4 产出,P5 回写实测时长与素材路径
│       ├── assets/
│       │   ├── audio/sc01.mp3 …
│       │   ├── images/sc01.png …(文件名=缓存键)
│       │   ├── extracted/      # 文档内嵌图片
│       │   └── bgm/
│       ├── subtitles.json / subtitles.srt / subtitles.ass
│       ├── final/output.mp4
│       └── qc_report.json
└── README.md
```

### 6.2 核心 JSON Schema(scenes.json,完整示例)

```json
{
  "$schema": "doc2video/schemas/scenes.schema.json",
  "style": { "name": "flat-illustration", "prefix": "扁平插画风格,深蓝+橙色配色,简洁构图,无文字", "negative": "text, watermark, words, letters" },
  "scenes": [
    {
      "id": "sc01",
      "chapter": "概述",
      "narration": "大家好,今天我们用五分钟解读这份报告的核心结论。",
      "est_duration_s": 5.1,
      "visual_desc": "演播台上一块大屏幕,屏幕上是报告的封面与标题",
      "image_prompt": "扁平插画风格,深蓝+橙色配色,简洁构图,演播台上一块大屏幕,屏幕上是报告的封面与标题,无文字,无水印",
      "aspect": "16:9",
      "audio_path": "assets/audio/sc01.mp3",
      "audio_duration_s": 5.32,
      "image_path": "assets/images/ab12cd….png",
      "image_status": "ok",
      "placeholder": false
    }
  ]
}
```

各阶段 Schema 均以 jsonschema 定义,P4 之后每次 LLM 输出必经 `jsonschema.validate`。

### 6.3 状态机与断点续跑

`state.json`:

```json
{
  "job_id": "20260827_ab12cd",
  "phase": "composing",
  "phases": {
    "P0": { "status": "done",    "input_hash": "…", "output_hash": "…", "attempts": 1, "finished_at": "…" },
    "P1": { "status": "done",    "input_hash": "…", "output_hash": "…", "attempts": 1 },
    "P2": { "status": "failed",  "input_hash": "…", "attempts": 3, "error": "…" }
  }
}
```

**恢复规则(`resume <job_id>`):**

1. 读取 state.json,找到第一个非 done 阶段;
2. 校验其**上游产物哈希**:未变 → 直接重跑该阶段;变了 → 下游全部作废重跑(级联失效);
3. P5 素材重跑前先查缓存,缓存命中不再调用供应商;
4. 阶段全部 done → 视为完成,`resume` 幂等返回。

---

## 7. 编排与自动化

### 7.1 CLI 设计

```bash
doc2video run <input.pdf|docx|md|txt> [options]      # 单文件全流程
    --style flat-illustration      # 视觉风格(styles.yaml 键)
    --aspect 16:9 | 9:16           # 横屏/竖屏
    --voice zh-CN-XiaoxiaoNeural   # TTS 音色
    --max-duration 600             # 成片最长秒数
    --bgm path/to/music.mp3        # 背景音乐(可选)
    --no-burn-subs                 # 不烧录硬字幕
    --llm local | cloud            # LLM 供应商组
doc2video resume <job_id>                              # 断点续跑
doc2video batch <dir> [--watch] [--jobs 1]             # 批量/监听目录
doc2video list                                          # 列出作业及状态
doc2video report <job_id>                               # 质检摘要
```

### 7.2 批处理与监听

- `batch`:扫描目录 → 去重(按已生成 manifest 的源文件哈希)→ 顺序执行(默认并发 1,因图像/LLM 资源受限;`--jobs N` 可调);
- `watch`:轮询目录(10s),新文件自动入队;正在处理的文件改名跳过。

### 7.3 错误处理与重试策略

| 环节 | 重试 | 降级 |
|------|------|------|
| LLM 调用 | 3 次,指数退避(2/4/8s);JSON 损坏先本地修复 | 章节跳过(报告标注) |
| TTS | 2 次 | 静音占位 |
| 图像 | 2 次 | PIL 占位图 |
| ffmpeg | 2 次 | 场景级失败 → 该场景静图直通(无动效) |
| 全局 | 阶段失败 → state=failed + 错误摘要;resume 可续 | — |

### 7.4 与 Hermes 环境集成(可选增强)

方案保持**纯 Python 独立运行**,不与 Hermes 耦合;可选集成路径:

- 封装为 Hermes skill(`doc2video`),让 Hermes 会话能一键调 `doc2video run …`;
- 讲稿/提示词生成可用 Hermes 子代理(deepseek 并发 3)做质量二次润色;
- FAL 图像调用可复用 Hermes 网关(或直连 FAL API,以解耦优先)。

---

## 8. 开发计划

### 8.1 里程碑与验收

| 里程碑 | 内容 | 验收标准 |
|--------|------|----------|
| **M0 骨架(1–2 天)** | uv 项目初始化、目录结构、CLI 入口、manifest/state 状态机、结构化日志、pytest 骨架、空管线跑通 | `doc2video run` 对一个假文档跑通全部空阶段;state.json 正确流转 |
| **M1 解析器(2–3 天)** | PDF/DOCX/MD 三格式解析 → parsed.json;层级推断;扫描件探测 | examples 下三份样例解析通过测试;结构树人工抽查正确 |
| **M2 理解+讲稿(2–3 天)** | LLMProvider(本地 Ollama 原生 API,think:false)+ 分块 map-reduce 摘要 + 讲稿生成 + schema 校验/修复/重试 | 样例文档产出合格讲稿(场景数/字数/时长达标);空 content 坑的测试通过 |
| **M3 素材生产(2–4 天)** | edge-tts、FAL 图像、缓存、并发信号量、失败降级 | 样例 Job 素材齐备;缓存命中验证(二次运行零 API 调用) |
| **M4 合成+字幕(3–5 天)** | ffmpeg Ken Burns/concat/loudnorm/烧录、faster-whisper 对齐、srt/ass | 产出可播放成片;字幕偏差 < 300ms;竖屏模式可用 |
| **M5 质检+发布(2–3 天)** | QC 检查、batch/watch、README、示例打包、端到端 golden 测试 | 三个典型场景(S1/S2/S3)端到端跑通;验收清单全绿 |

**总工期:** 单人 2–3 周;若用 Hermes 子代理并行(M2–M4 可并行开发),1–1.5 周。

### 8.2 任务分解要点(每任务 2–5 分钟粒度,实施时展开)

- M0:初始化 → 配置加载 → 状态机类 → CLI 骨架 → 空管线 → 日志;
- M1:类型探测 → PDF 文本提取 → 层级推断 → DOCX 样式映射 → MD 解析 → parsed schema → 样例 fixtures;
- M2:LLMProvider 抽象 → Ollama 原生适配(think:false)→ 摘要 prompt → map-reduce → 讲稿 prompt → JSON 修复器 → schema 校验;
- M3:edge-tts 适配 → 时长回写 → FAL 适配 → 缓存键 → 占位图 → 并发调度;
- M4:单场景合成 → Ken Burns 参数 → concat → loudnorm → bgm → whisper 对齐 → srt/ass → 烧录;
- M5:qc 各项 → batch → watch → README → golden 测试。

### 8.3 开发环境搭建

```bash
# 1. 项目初始化
cd D:/Hermesdcu/doc2video
uv init --python 3.11
uv add pymupdf python-docx markdown-it-py edge-tts httpx pyyaml pydantic \
       jsonschema faster-whisper pytest pytest-asyncio
uv add --dev ruff

# 2. ffmpeg
winget install ffmpeg   # 或 choco install ffmpeg

# 3. Ollama(已装,仅确认并行配置)
ollama list
# 环境变量 OLLAMA_NUM_PARALLEL=2 (启动 Ollama 时设置)

# 4. 密钥
# FAL_KEY 写入 .env(uv 项目可用 python-dotenv 加载,勿提交)
```

### 8.4 开发约定

- TDD:每模块先写失败测试再实现(见 plan/test-driven-development 技能);
- 每任务一 commit,conventional commits(`feat:`/`fix:`/`test:`);
- LLM/TTS/图像调用在测试中一律 mock,仅 golden 端到端测试才打真接口(标记 `@pytest.mark.e2e` 默认跳过);
- ffmpeg 命令全部模板化存放,单元测试断言生成的命令行字符串。

---

## 9. 质量保障与验收

### 9.1 测试策略

| 层级 | 内容 | 工具 |
|------|------|------|
| 单元 | 解析器(层级推断)、时长估算、字幕插值、缓存键、JSON 修复器、ffmpeg 命令模板 | pytest |
| 契约 | 所有 JSON 产物过 jsonschema | jsonschema |
| 集成 | 各阶段用 fixtures 串联,供应商 mock | pytest-asyncio |
| 端到端 | 3 类样例文档完整跑通(golden),供应商可选打真接口 | pytest(标记 e2e) |

### 9.2 验收标准(量化)

| # | 标准 | 指标 |
|---|------|------|
| AC-01 | 三种输入格式端到端产出可播放成片 | 3/3 样例通过 |
| AC-02 | 字幕-语音同步 | 平均偏差 < 300ms(抽查 10 句) |
| AC-03 | 成片规格 | H.264 + AAC,1080p/25fps,ffprobe 校验通过 |
| AC-04 | 讲稿质量 | 场景数/时长符合约束;人工抽听 1 条无事实错误、无重复 |
| AC-05 | 断点续跑 | 中断 P5 后 resume:已完成阶段跳过、素材缓存命中 |
| AC-06 | 稳定性 | 批处理 10 个文件连续跑,零未处理异常(降级记录除外) |
| AC-07 | 性能 | 5000 字文档端到端 ≤ 10 分钟(不含云端排队) |
| AC-08 | 成本 | 同输入二次运行:云端 API 调用数 = 0(纯缓存) |

### 9.3 质检指标定义

静音段:连续 < -50dB 超过 2s;黑帧:平均亮度 < 16/255;字幕覆盖:每场景至少 1 条字幕且总时长 ≥ 场景音频 90%。

---

## 10. 风险与对策

| # | 风险 | 影响 | 对策 |
|---|------|------|------|
| R1 | LLM 输出 JSON 损坏/不稳定 | P3/P4 产出不可用 | schema 校验 + 本地修复器 + 重试 3 次 + 温度调低(0.3) |
| R2 | TTS 实际时长与估算偏差大 | 画面-音频不同步 | 以实测时长驱动合成;±10% 用 atempo 微调兜底 |
| R3 | 图像生成失败/排队超时 | 场景无画面 | 重试 + PIL 占位图降级 + 报告标注 |
| R4 | 长文档超 LLM 上下文 | 生成失败或遗漏 | 分块 map-reduce;超长自动分集 |
| R5 | 扫描件无文本层 | P1 无产出 | 探测 → OCR 降级(rapidocr);OCR 不可用则明确报错 |
| R6 | 本地 Qwen3 空 content 坑 | P2/P3 静默失败 | 走原生 /api/chat + think:false;空 content 检测 + 云端降级(专项测试) |
| R7 | ffmpeg 版本/滤镜差异 | 合成异常 | 锁定 ffmpeg 6.x;CI 冒烟;命令模板化 |
| R8 | 云端成本失控 | 订阅额度消耗 | 素材缓存 + 本地模型优先 + 并发限流 |
| R9 | 图像中出现乱文字 | 观感差 | 提示词加 negative + 风格模板固定无文字;QC 抽样(可选 VLM 检测) |

---

## 11. 附录

### 附录 A.端到端示例(500 字短文)

**输入 `examples/demo.md`:**

```markdown
# 咖啡因如何影响睡眠
咖啡因是世界上最常用的精神活性物质……
(正文 3 段,约 500 字)
```

**P3 产出 `script.json`(节选):**

```json
{ "scenes": [
  { "id": "sc01", "narration": "大家好,今天我们聊聊咖啡因和睡眠的关系。很多人靠咖啡提神,却不知道它悄悄偷走了你的深度睡眠。", "est_duration_s": 11.3 },
  { "id": "sc02", "narration": "咖啡因的半衰期大约是五到六个小时。也就是说,下午三点喝的那杯咖啡,到晚上九点还有一半留在你身体里。", "est_duration_s": 12.4 },
  { "id": "sc03", "narration": "所以,如果你晚上睡不好,试试把最后一杯咖啡提前到中午之前。今天就到这里,我们下期再见。", "est_duration_s": 10.8 }
]}
```

**P5 后 scenes.json 回写:** 每个场景 `audio_duration_s`(实测,如 11.82/12.95/11.30)、`image_path` 指向缓存图像。

**最终目录:** `workspace/20260827_ab12cd/final/output.mp4`(时长 ≈ 37s)+ subtitles.srt/ass + qc_report.json。

### 附录 B.关键 Prompt 模板

**B.1 讲稿生成(P3,`prompts/script_writer.txt`):**

```
你是专业视频讲稿撰写人。根据【文档摘要】写口播讲稿,要求:
1. 全片 ≤ {max_scenes} 个场景,每场景 60–180 字(约 15–40 秒);
2. 口语化中文,短句为主,避免书面长句和术语堆砌;
3. 第 1 场景是开场白:问候 + 文档标题 + 看点预告;
4. 最后 1 场景是结尾:总结 + 道别;
5. 场景之间逻辑递进,不重复信息;
6. 只输出 JSON,格式:
   {"scenes":[{"id":"sc01","chapter":"…","narration":"…","est_duration_s":…}]}
7. est_duration_s 按每字 0.222 秒估算(4.5 字/秒),保留一位小数。

【文档摘要】
{doc_summary}
【要点】
{key_points}
【章节计划】
{chapter_plan}
```

**B.2 分镜画面描述(P4,`prompts/scene_planner.txt`):**

```
为每个讲稿场景写画面描述与图像提示词:
1. visual_desc:一个明确的画面(主体+动作+环境),禁止出现画面中的文字内容;
2. image_prompt = 风格前缀 + visual_desc + "无文字,无水印";
3. 风格前缀:{style_prefix}
4. 只输出 JSON:{"scenes":[{"id":"…","visual_desc":"…"}]}(其余字段由程序合并)
【讲稿】
{scenes_json}
```

**B.3 摘要(P2,`prompts/summarizer.txt`):** 分块摘要 + 归并模板(输出 doc_summary/key_points/chapter_plan 三字段 JSON),此处略。

### 附录 C.ffmpeg 命令集

**C.1 单场景(Ken Burns + 音频 + 淡入淡出),见 4.6 正文示例。**

**C.2 concat 拼接:**

```bash
# scenes.txt 内容:file 'scene01.mp4' / file 'scene02.mp4' …
ffmpeg -y -f concat -safe 0 -i scenes.txt -c copy joined.mp4
```

**C.3 响度归一 + 背景音乐混音:**

```bash
ffmpeg -y -i joined.mp4 -stream_loop -1 -i bgm.mp3 \
  -filter_complex "[0:a]loudnorm=I=-16:TP=-1.5[a0];[1:a]volume=0.12,afade=t=out:st=TOTAL-3:d=3[a1];[a0][a1]amix=inputs=2:duration=first[a]" \
  -map 0:v -map "[a]" -c:v copy -c:a aac -b:a 128k with_bgm.mp4
```

**C.4 硬字幕烧录 + 最终编码:**

```bash
ffmpeg -y -i with_bgm.mp4 -vf "ass=subtitles.ass:fontsdir=assets/fonts" \
  -c:v libx264 -preset medium -crf 20 -c:a aac -b:a 128k -movflags +faststart output.mp4
```

### 附录 D.评审记录

| 评审方 | 日期 | 结论 | 修订内容 |
|--------|------|------|----------|
| gpt-5.6-sol(技术审查) | 待执行 | 待填写 | 待填写 |

---

*本文档即开发依据。任何实现细节冲突时以本文档为准;文档修订需同步更新版本号与评审记录。*
