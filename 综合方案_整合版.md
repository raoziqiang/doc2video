# doc2video 综合改进方案（整合四版审查结论）

- 编制日期：2026-08-28
- 整合来源：
  - `review_report.md` —— 《技术方案.md》v0.1 评审（结论：不通过；P0-01~11 / P1-01~12 / P2-01~08）
  - `review_report_v02.md` —— v0.2 复审（结论：不通过；阻断项 H-01~H-10、P1 吸收度核对）
  - `review_report_code.md` —— 代码审查 v0.1（结论：NO-GO；B-01~08 阻断 / H-01~08 高危 / M、L 中低危）
  - `review_report_v03.md` —— 最新代码审查（10 项问题**全部修复**，164 测试全绿，`ruff` 全绿）
- 当前基线：`uv run pytest` **164 passed**；`ruff check src tests scripts` 全绿；M0 Spike 证据已入库（`docs/spikes/`：FAL 契约快照、ffmpeg 验证、ollama/whisper 基准）

---

## 一、项目现状与核心问题总结

### 1.1 演进脉络：从"文档评审"到"代码审查"的四轮收敛

| 轮次 | 对象 | 结论 | 核心产出 |
|---|---|---|---|
| 第 1 轮 | 技术方案 v0.1 | 不通过 | 11 项 P0 硬伤（阶段循环依赖、事实瓶颈、假 Schema、状态机不全、隐私边界缺失等） |
| 第 2 轮 | 技术方案 v0.2 | 不通过 | 确认架构方向已显著纠偏；指出契约未提交、FAL 端点未冻结、sidechain 接反等 10 项阻断 |
| 第 3 轮 | 实现代码 | NO-GO | 工程质量优良，但"接口存在、实现缺失"（B-01~B-08）成为主要矛盾 |
| 第 4 轮 | 实现代码（最新） | **问题全修复** | 10 项缺陷修复 + 8 个回归测试；剩余缺口全部归入本方案的待办清单 |

### 1.2 已确认落实的设计承诺（代码验证，可依赖）

- **崩溃安全状态机**：原子提交（tmp→fsync→os.replace）、Job 跨进程锁、`events.jsonl` 审计、完整转移表 + `require_transition`；`failed/cancelled` 续跑已修复（v03 问题 1）
- **契约驱动**：16 个契约 `extra=forbid` + 自动生成 Draft 2020-12 Schema（`contracts/` + `schemas/`），P4/P5 产物分离（scene_plan vs assets_manifest）
- **fail-closed 交付门禁**：P8 硬门禁不过不晋升 final（硬链接晋升）；offline 出现云调用即硬失败（v03 新增）
- **隐私三档**：默认 offline；外发审计 `egress_manifest.json` 真实写入 + `max_cloud_calls_per_job` 配额强制（v03 闭环，对应历史 B-04/H-09）
- **时间轴与响度**：`render_timeline` 由 P5 提交（P6/P7/P8 共用，修复 H-07 循环）；两遍 loudnorm 实测入 render_manifest；BGM sidechain ducking 方向正确且已接线（v03）
- **CLI 选项不再静默**：`--bgm`、`--no-burn-subs`、`--max-duration` 全部有消费方；`--llm cloud` fail-closed 拒绝；`--jobs>1` 显式降级警告（v03 闭环 B-01~B-03）
- **resume 参数保真**：`run_options.json` 持久化与恢复；manifest 缺失友好报错（v03 闭环 B-06/H-02）
- **输入安全**：P0 DOCX ZIP bomb 防护、magic 嗅探、路径 containment、`shell=False`、ASS/concat 转义
- **事实一致性**：P3 `check_claims`（quote 逐字 + 关键 token）；表格块寻址按类型选取（v03 修复 H-05）

### 1.3 仍然开放的核心问题（按四版报告收敛）

**A. 功能最后一公里（第 3 轮遗留，未闭合）**

| 编号 | 问题 | 现状证据 |
|---|---|---|
| B-05 | `native_marks` 无生产方：edge-tts 未收集 WordBoundary，P6 三级策略退化为 CPU whisper 唯一路径 | grep 确认 `p5_assets.py` 无 SubMaker/WordBoundary；消费方 `p6_subtitles.py` 已实现 marks → whisper → 字符比例三级兜底，**仅缺生产方** |
| B-07 | FAL request_id 不预持久化：提交→响应之间崩溃，恢复后重新提交（重复计费） | `_record_egress` 在成功后才记录，无 pending intent；注意 edge-tts 无幂等/查询接口，属 `unknown_submit` 窗口，见 S1.2 分级策略 |
| B-08 | 成本上限零执行：`max_cost_per_job/day` 配置存在但无消费方（调用次数配额已生效） | v03 遗留项 1 |

**B. 代码高危项（第 3 轮遗留）**

| 编号 | 问题 |
|---|---|
| H-01 | `stage_fingerprint` 为桩级（缺输入哈希/Prompt 哈希），resume 不比对指纹 → 改配置后续跑不失效旧产物 |
| H-03 | Ollama `think` 放在 options 内会被忽略，应为顶层参数；`max_output_tokens` 未发送 |
| H-04 | P2 `NEEDS_REVIEW` 异常被 runner 统一按 failed 处理（退出码 2），应为 needs_review（退出码 3） |
| H-06 | P8 事实检查只验 fact ID 存在，不验数字/单位一致性；P2 coverage.uncovered 不进门禁 |
| M-01~07 | OCR 逐页重载实例、加粗误判标题、PDF 内嵌图不提取、P9 draft_meta 路径未重写、P6 O(n²) 等 |
| L-01~04 | EventLog 全量重读（O(n²)）、revision 恒为 1、`__import__` 反模式、BGM/字体未快照入 Job |

**C. 设计与验收层缺口（第 1、2 轮提出，实现中未落地）**

1. **QC 阈值矩阵不完整**：缺最终 LUFS/true peak/削波、静音比例、黑帧、字幕覆盖率、占位/重复图上限的 pass/warn/fail → publish 完整矩阵（对应 P1-06、v02 H 项）
2. **字幕对齐无真值验收**：`<300ms` 承诺需要人工真值集上的 P50/P95/max/覆盖率；forced aligner 未选型冻结（P0-10）——本方案**显式延期至二期**（当前 edge-tts 有原生 marks、whisper 可兜底，aligner 路线暂无消费场景，见 S4）
3. **发布 smoke 门禁未绑定执行**：真实 Provider（Ollama/edge-tts 音色/FAL/ffmpeg+CJK/whisper）+ 30~60 秒完整成片的不可跳过 gate 仅有制度设计，无执行机制与结果留存（P0-11）
4. **故障注入未成体系**：写前/写中/写后终止、磁盘满、429/5xx、未知提交窗口、双 resume、缓存损坏等缺测试（P0-11）
5. **隐私可执行性**：Provider 信任矩阵（发送字段/留存/区域）、数据分类、授权 receipt 未落契约（v02 P0-08）
6. **运维配额**：磁盘/工作区/缓存配额、TTL、GC、日志轮转无默认值（P1-11）
7. **性能 SLO 无目标值**：冷热缓存 × 长短文档 × 横竖屏的 P50/P95 缺数字（P2-05）
8. **许可证风险未处置**：PyMuPDF AGPL/商业双许可、ffmpeg GPL build、打包字体/音色/生成素材权利——若分发闭源产物即发布阻断（P1-10）

### 1.4 一句话现状判断

> **架构与工程骨架已达到同类项目上游水平（164 测试全绿、崩溃安全、契约严格、隐私默认保守），但"可信交付"的三块拼图缺失：字幕主路径（native marks）、远端提交的计费安全（request_id 预持久化 + 成本配额）、以及可机器判定的发布门禁（QC 阈值矩阵 + 真实 smoke gate）。补齐这三块，项目即具备从"可跑通"到"可发布"的条件。**

---

## 二、产品能力目标

### 2.1 三档交付级别（沿用技术方案 8.1，按现状修订）

| 级别 | 定义 | 当前达成度 | 剩余差距 |
|---|---|---|---|
| **PoC（快乐路径）** | 文本类输入 → 可播放成片，单 Provider | ✅ 已达成 | — |
| **MVP（功能完整）** | 四类输入、真实 Provider、断点续跑、量化验收 | ⚠️ ~80% | B-05/H 项、QC 阈值、真值验收 |
| **发布级（无人值守）** | 发布 smoke 门禁、故障注入、成本/隐私全闭环 | ⚠️ ~50% | 1.3 节 C 组全部 |

### 2.2 能力目标（按优先级）

1. **内容可信**：讲稿每句可追溯到源块；数字/日期/专名/单位一致性经双重校验（P3 生成时 + P8 复验）；证据覆盖不足不放行
2. **交付可信**：成片音画字幕完整、响度达标、编码规格合规；`needs_review/failed/preview` 永不晋升 final
3. **成本可控**：单作业/单日成本上限可执行；远端请求幂等、崩溃恢复不重复提交
4. **隐私可审计**：三档模式全链路贯通；任何外发都有审计记录；敏感文档不经授权不走云路径
5. **可运维**：磁盘/缓存/日志有配额；失败可诊断（事件日志 + 退出码语义正确）；环境可复现（doctor 全绿）

---

## 三、工程改进优先级

### P0 阻断级（合入下一个发布前必须完成）

| 序号 | 工作项 | 来源 | 理由 |
|---|---|---|---|
| 1 | **B-05 native_marks 生产**：edge-tts 收集 WordBoundary/SentenceBoundary 落盘 `marks.json`，P5 写入 narration，P6 主路径消费 | code H/B | 性能（省去 5~15 分钟 CPU whisper）+ 字幕质量；方案 4.7 主路径不可达 |
| 2 | **B-07 request_id 预持久化**：FAL 提交前原子写 pending intent（prompt hash + client_request_uuid），提交后补记；恢复时只查询不重提 | code B / v01 P0-11 | 真金白银的重复计费风险 |
| 3 | **B-08 成本配额落地**：基于预估单价（config 冻结每 SKU 单价）做事前预算检查 + egress 累计估算，超限硬失败；真实计费接口接入后切换 | code B / v03 遗留 | 成本护栏失守 |
| 4 | **H-01 指纹完整化**：`stage_fingerprint` 纳入有序输入哈希、Prompt 哈希、模型/配置指纹；resume 比对指纹并从最早 dirty 节点级联失效 | code H / v01 P0-04 | "改配置续跑"语义正确性 |
| 5 | **H-04 NEEDS_REVIEW 通道**：runner 识别 `NEEDS_REVIEW:` 异常 → needs_review 状态 + 退出码 3 | code H | 自动化系统不误判人工复核为程序故障 |
| 6 | **H-03 Ollama `think` 顶层参数**：`think` 移出 options 提为顶层参数，补发 `max_output_tokens`（num_predict） | code H | 改动约 1 行 + 1 测试，成本极低；`think` 被忽略时思考输出挤占预算，存在讲稿截断风险，故升档至 P0 |

### P1 高危（下一个迭代）

| 序号 | 工作项 | 来源 |
|---|---|---|
| 7 | H-06 P8 复验数字/单位一致性；coverage.uncovered 进门禁（能力目标 #1「内容可信」的直接门禁，**建议与 S1 并行**而非排在成本门禁之后） | code H / v01 P0-02 |
| 8 | **QC 阈值矩阵**：时长、静音、黑帧、字幕覆盖、占位率、LUFS/true peak/削波 → pass/warn/fail → publish 完整映射，全部可机器判定 | v01 P1-06 / v02 |
| 9 | M-06 P9 `draft_meta_info.json` 路径重写；L-02 revision 真实递增；L-03 移除 `__import__` 反模式；**L-04 BGM/字体快照入 Job**（路径 containment 相关，v02 §6.2.9 提出） | code M/L |

### P2 择机（随迭代消化）

| 工作项 | 来源 |
|---|---|
| M-01 OCR 实例复用；M-02 加粗判标题加约束；M-03 PDF 内嵌图提取；M-07 P6 O(n²)；L-01 EventLog 增量 seq | code |
| 缓存并发"内容替换—marker 未写"窗口加固（`--jobs>1` 前置条件） | v03 遗留 |
| `--jobs>1` 批量并行；`--llm cloud` 云 provider | v03 遗留 |

---

## 四、分阶段实施计划

> 与技术方案的 M0~M7 对应关系：M0（契约+Spike）~M5 的主体已实现，以下四个阶段是**收敛剩余差距**的实施路径，总工期估算 4~6 周（不含发布级风险缓冲）。

### 阶段 S1：最后一公里闭环（约 1 周，对应 P0 序号 1~6）

- **S1.1 native marks 主路径**（2~3 天）
  - edge-tts 请求开启 SubMaker/WordBoundary 流 → `audio/marks.json`
  - **marks 契约化**（v02 §3.11 第 5 点）：`native_marks` 写入 `contracts/assets.py`，定义时间基准（音频内相对时间）、与 `scene_start + lead` 的叠加规则、缺失/重叠/越界 marks 的处理语义（越界裁剪、重叠去重、缺失降级兜底）
  - P5 narration 资产写入 `native_marks` 字段；P6 优先消费，缺失才走 whisper 兜底（兜底需记录置信度）
  - 测试：真实音色冒烟 + 单测断言 marks 覆盖率；14 分钟输入的耗时对比记录
- **S1.2 远端提交安全**（2 天）
  - FAL 提交前写 pending intent（含 prompt hash、client_request_uuid）；响应后补 request_id；resume 对已有 request_id 只查询不重提（at-most-once 语义）
  - **edge-tts 分级策略**（v01 P0-11）：无幂等/查询接口，崩溃重发属 `unknown_submit` 窗口，语义为 at-least-once；不计费，容忍重发，但每次调用必须入 egress 审计，不得静默
  - 成本配额：config 冻结 SKU 单价 → 提交前预算检查 + egress 累计 → 超限 `AssetError`
- **S1.3 状态机收尾**（1~2 天）
  - 指纹纳入输入哈希 + Prompt 哈希；resume 全量重验 + 级联失效（含 P0 序号 6 的 `think` 参数修复）
  - `NEEDS_REVIEW` 异常 → needs_review（退出码 3）；补对应测试
- **S1.4 文档同步**（0.5 天）：`技术方案.md`/README 中 egress、request_id、run_options、`--bgm`/`--no-burn-subs` 等已随 v03 闭环的内容更新为完成时表述，消除「宣称—事实」漂移（v02 反复警示项，对应 §7 第 5 条）
- **退出条件**：既有测试零回退 + 新增回归；真实 edge-tts 冒烟产出带 marks 的成片；构造崩溃注入证明不重复提交

### 阶段 S2：可机器判定的质量门禁（约 1~2 周）

- **S2.1 QC 阈值矩阵**（2~3 天）：把 P8 检查项全部参数化进 `config/qc`（阈值、级别、是否发布），`qc_report.json` 输出逐项判定 + 总发布决定；`succeeded_with_warnings` 的发布语义写入矩阵
- **S2.2 事实与覆盖门禁**（1~2 天）：P8 复验数字/单位；uncovered 块超阈值 → needs_review
- **S2.3 字幕真值验收**（2~3 天）：建立 20~30 句人工标注真值集，定义句起止误差 P50/P95/max/覆盖率；附标注规范 + 复核机制（标注后隔日自检复核一遍）；AC 以真值集为准，不达标删除 `<300ms` 硬承诺改为预览级
- **S2.4 端到端冒烟与接线测试**（2 天）：小输入 → 全管线 → 断言最终 mp4 含音轨/字幕轨/时长/响度；每个 CLI flag 一个"传参后断言下游行为改变"的接线测试
- **S2.5 M/L 项消化**（并行）：M-06、L-02、L-03、L-04（BGM/字体快照入 Job，见 P1 序号 9）
- **S2.6 preview 隔离**（1~2 天）：实现 `preview` 子命令（当前为 `cmd_not_implemented`），preview 产物写独立目录、带不可发布标记，P8 永不晋升（v02 P0-07 要求）；静音降级仅允许 preview
- **退出条件**：QC 矩阵 100% 可机器判定；E2E 冒烟入库为回归；真值集报告入库

### 阶段 S3：发布硬化（约 2~3 周，对应方案 M6~M7）

- **S3.1 发布 smoke gate 绑定执行**（3~4 天）：`scripts/release_gate.py` 本体已实现（419 行，含 `--media-smoke`/`--live` 分级与 fail-closed），本项缺口是**绑定与强制执行机制**——发布流程强制先跑 gate、结果落 `docs/release/` 并与候选产物 digest 绑定、无手工绕过入口（而非重新开发脚本）
- **S3.2 故障注入矩阵**（3~4 天）：写前/写中/写后终止、manifest 已提交 state 未更新、磁盘满、JSON 截断、哈希不符、429/5xx、双 resume、缓存损坏——逐项断言"不产生伪成功、截断产物不被接受、已有 request_id 不重提"
- **S3.3 隐私可执行化**（2~3 天）：Provider 信任矩阵（edge-tts/FAL 发送字段、留存、区域）入 `docs/`；敏感作业授权流程与记录；日志/错误响应脱敏
- **S3.4 运维配额与 SLO**（2~3 天）：磁盘/工作区/缓存配额 + `cache gc`；性能基准（冷/热 × 短文/5000 字 × 横/竖）记录 P50/P95 入库，写进验收
- **S3.5 许可证决策**（1 天）：按分发模式（内部工具 / 闭源分发 / 开源）输出许可证结论：PyMuPDF 商用、ffmpeg GPL build、字体/音色权利；必要时替换依赖或调整分发形态——**该项是发布阻断条件，必须在首个对外交付前完成**
- **退出条件**：release_gate 全绿且不可手工绕过；注入矩阵全过；许可证结论落文档

### 阶段 S4：能力扩展（择期，不阻塞发布）

- `--jobs>1` 批量并行（前置：缓存并发窗口加固 + Job 锁复核）
- `--llm cloud` provider（前置：隐私授权流程 + egress 覆盖）
- 解析增强：多栏/跨页表格/DOCX 高级结构、OCR 质量
- 视觉一致性：固定 seed 策略、style bible、重复图检测
- 超长分集（二期，维持"超限拒绝或人工拆分"现状）
- **forced aligner 选型与接入**（一期显式延期：当前 marks + whisper 兜底已覆盖默认链路；引入无原生 marks 的 TTS provider 时再冻结选型，见 1.3-C2）

---

## 五、关键验收标准（机器可判定）

| # | 验收项 | 判定方式 | 归属阶段 |
|---|---|---|---|
| AC-1 | 测试基线不回退 | `uv run pytest` **既有用例零回退**（不允许删旧用例凑数）+ 每新增能力必带回归；当前基线 164 项 | 全程 |
| AC-2 | 静态检查 | `ruff check src tests scripts` 全绿 | 全程 |
| AC-3 | native marks 主路径 | 按场景统计：≥95% 场景的 cues 来自 marks（低于阈值的场景 → warn 入 qc_report，连续 2 场景低于阈值 → fail）；whisper 兜底有置信度记录 | S1 |
| AC-4 | 不重复计费 | 注入"提交后崩溃"，恢复后无新提交（egress 无新 request） | S1 |
| AC-5 | 成本护栏 | 构造超预算作业 → 硬失败且退出码正确 | S1 |
| AC-6 | 指纹失效 | 修改 config 后 resume → 受影响阶段级联 invalidated | S1 |
| AC-7 | QC 矩阵 | 所有检查项有阈值/级别/发布映射；构造违规样本逐项触发 | S2 |
| AC-8 | 字幕误差 | 真值集上报告 P50/P95/max/覆盖率；达标则保留 <300ms，否则降级口径 | S2 |
| AC-9 | E2E 冒烟 | 小输入全管线：最终 mp4 含视频/音频/字幕，时长±5%（基准为 `render_timeline.total_s`，含 lead/trail），响度达标 | S2 |
| AC-10 | CLI 接线 | 每个 flag 一个接线测试全绿 | S2 |
| AC-11 | 发布 smoke | release_gate 清单全绿，结果与候选产物 digest 绑定，不可手工跳过 | S3 |
| AC-12 | 故障注入 | 注入矩阵全过，无伪成功、无截断产物被接受 | S3 |
| AC-13 | 隐私审计 | offline 作业零云调用（P8 门禁）；approved_cloud 作业 egress 清单与调用一一对应 | S3 |
| AC-14 | 性能基线 | 冷/热 × 文档规模 × 画幅的 P50/P95 入库；目标值写入验收 | S3 |
| AC-15 | 许可证 | 分发形态对应的许可证结论文档存在且经法务/负责人确认 | S3（发布阻断） |

---

## 六、测试与质量保障建议

1. **补齐两类覆盖盲区**（第 3、4 轮反复暴露的模式）：
   - **生产方缺失**：测试手工构造产物掩盖"无人生产"（如 native_marks）。对策：E2E 冒烟断言真实产物链，禁止关键路径用手工构造替代。
   - **接线缺失**：选项被收集但无消费方。对策：每个 CLI flag 强制配接线测试；新增选项的 PR 必须证明下游行为变化。
2. **失败路径一等公民**：每个新状态/退出码必须有失败注入测试（v03 的 `StateError` 崩溃正是该盲区）。
3. **故障注入框架化**：把 S3.2 的注入点沉淀为可复用夹具（截断文件、磁盘满模拟、429 桩、双进程抢锁）。
4. **真实 Provider 分级**：日常开发默认 mock；发布候选强制真实 smoke（付费项控制预算，每次 <1 美元级）；结果留存进 `docs/release/`。
5. **真值集治理**：字幕真值集版本化入库，随验收报告一起留存；指标不达标时修改口径而非放宽阈值。
6. **基准入库**：性能/显存/耗时基准（M0 已有 ollama_bench/whisper_bench 先例）延续为每次发布候选的记录项。
7. **审查纪律**：每阶段结束跑一次"宣称—实现"一致性核对（grep 验证配置项/选项是否有消费方），把第 3 轮的审查方法固化为自检脚本。

---

## 七、后续风险与待跟进事项

| # | 风险 | 级别 | 检测信号 | 缓解 |
|---|---|---|---|---|
| R1 | FAL/edge-tts 端点、Schema、音色、计费漂移 | 高 | contract smoke 失败；响应字段缺失 | 契约快照 + 能力 preflight，漂移即 fail closed（M0 已建快照机制，需绑定到发布门禁） |
| R2 | 重复计费/孤儿请求（服务端自动重试 + 客户端重试叠加） | 高 | egress 中出现重复 request_id；费用异常 | 幂等头（已加）+ intent 预持久化（S1）+ 费用封顶（S1） |
| R3 | 许可证（PyMuPDF AGPL、ffmpeg GPL、字体/音色）在对外分发时成为阻断 | 高 | 分发形态决策 | S3.5 提前决策；不解决则限定内部使用 |
| R4 | 字幕 <300ms 硬指标无真值支撑 | 中 | 真值集测量结果 | 有真值才承诺；否则降级为预览级口径（不造假指标） |
| R5 | LLM 幻觉/遗漏进入成片 | 中 | coverage/facts 门禁 | P8 复验（S2.2）+ 高风险领域保留人工复核 |
| R6 | 磁盘/缓存无限增长（批处理/长期运行） | 中 | 工作区体积 | 配额 + GC（S3.4） |
| R7 | 缓存并发窗口在并行化后暴露 | 中 | `--jobs>1` 实施 | 并行化前加固（S4 前置） |
| R8 | 成本配额基于估算单价，与真实账单存在偏差 | 低 | 账单对账 | 先以次数配额为主，接入计费接口后切换真实值 |
| R9 | 单人开发工期乐观（技术方案 PoC 3~4 周 vs 当前已消耗进度） | 中 | 里程碑偏差 | 每阶段按退出条件验收，不压缩 S1/S3 |

**待跟进事项清单（非代码）**：
1. 字幕真值集标注（需人工，20~30 句，约 0.5 天）
2. 发布候选的付费 smoke 预算审批（每候选 <1 美元级）
3. 分发形态决策 → 许可证结论（S3 阻断项，**已决策 2026-08-28：形态 C 开源发布，整体许可 AGPL-3.0，见 `docs/licenses.md` 与 `LICENSE`**）
4. 医疗/法律/金融等高风险领域的公开发布审批流程（维持人工复核，不做全自动承诺）
5. 本方案与各轮报告的编号对照维护：后续每轮审查结论应更新 `综合方案_整合版.md` 的达成度列，避免再次出现"宣称已落实"与仓库事实不符（v02 已警示过）

---

*修订记录：v1.1（2026-08-28）——按方案审查意见修订：H-03 升档 P0；S1.1 补 marks 契约子项；S1.2 补 edge-tts at-least-once 分级策略；S1.4 新增文档同步项；S2.6 新增 preview 隔离；S3.1 更正 release_gate 现状描述（本体已实现，缺绑定机制）；forced aligner 显式延期至二期；AC-1/AC-3/AC-9 口径修正。*

*本方案为四版审查报告的收敛产物：第 1、2 轮的文档级问题已在 v0.2.1 方案与代码中大部分闭合；第 3 轮的 8 项阻断已闭合 6 项（剩余 B-05/B-07/B-08 进 S1）；第 4 轮 10 项已全部修复。后续以本方案的阶段退出条件为唯一推进依据。*
