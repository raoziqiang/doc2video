# 2026-08-27

## 代码审查（src/doc2video 38 文件，对照技术方案 v0.2.1）

- 产出报告：`review_report_code.md`，结论 **不通过（NO-GO）**
- 基线：pytest 156 passed / ruff 全绿——问题在功能接线而非表面质量
- P0 阻断项（8 个）：
  - B-01 --bgm 未接线（build_audio_mix_filter 死代码，仅测试调用）
  - B-02 --no-burn-subs 永远烧录
  - B-03 --max-duration 无执行点
  - B-04 egress_manifest.json 无生产方（唯一"写入方"在 tests）
  - B-05 native_marks 无生产方 → P6 退化为 CPU whisper 唯一路径
  - B-06 cmd_resume 重建 RunOptions 丢失原始 opts
  - B-07 FAL request_id 不预持久化（崩溃重提重复扣费）
  - B-08 max_cost/max_cloud_calls 配置零执行
- 高危 8 项：指纹桩级（resume 不比对）、manifest 缺失崩溃、Ollama think 在 options 内应顶层、P2 NEEDS_REVIEW 被当 failed、source_block_ids[0] 无类型校验、P8 事实数字不复验、token_counter 运行时下载（offline 破防）、doctor 双跑+条件笔误
- 正面确认：原子提交/JobLock/两遍 loudnorm（H-04）/sidechain 方向（H-03）/render_timeline 归 P5（H-07）/fail-closed 离线/硬链接晋升/契约 extra=forbid 均真实落实
- 测试盲区：大量手工构造产物掩盖"生产方缺失"（native_marks、egress、BGM 滤镜均只被单测调用）→ 建议加端到端冒烟 + CLI flag 接线测试
